# Harrison Family Frontend Template

This directory intentionally remains empty. The real ETL output (person/media JSON) contains PII and must live in `workspace-local/harrisonfamily-frontend/`.

When running demos or sharing architecture, generate synthetic data and place it here before copying into the repo.
